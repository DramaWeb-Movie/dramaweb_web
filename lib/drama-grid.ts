/** Same card widths as home page: 160 → 180 → 200 → 220px so card size is consistent everywhere. */
export const DRAMA_CARD_GRID =
  'grid grid-cols-[repeat(auto-fill,160px)] sm:grid-cols-[repeat(auto-fill,180px)] md:grid-cols-[repeat(auto-fill,200px)] lg:grid-cols-[repeat(auto-fill,220px)] gap-4 md:gap-6 justify-start';
